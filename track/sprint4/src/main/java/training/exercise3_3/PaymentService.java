package training.exercise3_3;
/*
public class PaymentService {
    void processPayment(Payment payment) {

        if (payment.isNull || payment.getAmount <= 0) {
            throw new IllegalArgumentException("Invalid Payment details provided");
        }

        if (payment.hasCardDetails()) {
            cardProcessor.process(payment);
        } else if (payment.isCash()) {
            cashProcessor.process(payment);
        } else {
            throw new UnsupportedPaymentMethodException("No valid payment method provided.");
        }
    }
}
*/